# CPU TLB seed-state verification

The opt-in `ENABLE_TLB_LOAD` CPU slice adds full 32-bit DCMP (SPR 977), ICMP (981), and RPA (982) seed registers. This round verifies their architectural register behavior only; no `tlbld` or `tlbli` instruction is included in this slice.

`make -C sim test-tlb-seed-decode test-core-tlb-seed` passes with strict Verilator warnings. The independent decode bench reports **2,062 checks**: all 32 GPR fields for three selectors and three XO forms (MFSPR 339, 603e MFTB alias 371, MTSPR 467), with Rc=0 accepted (288 cases) and Rc=1 rejected (288 cases); 12 adjacent-selector forms are also rejected. Hardcoded instruction words anchor the reversed SPR field layout. A disjoint XO371 LR-selector case checks that enabling TLB seed state does not broaden unrelated alias decode. The default and supervisor-only feature-off decoders reject all new selector forms; a combined BAT, segment, and TLBIE configuration agrees on seed decoding.

The actual `ppc_core` bench reports **1,417 enabled checks** and **88 default-off checks**. Its literal program checks reset-zero state, full-width committed writes/readback for all three selectors, old full r0 as the ICMP write source, both MFSPR and MFTB read forms, and an existing SRR1 write/read operation. It holds the DCMP write at retirement for eight cycles and confirms the register remains unchanged until the exact retire edge. Full redirects cancel a pending seed write or read: an accepted cut asserts `special_cancel` and prevents completion acceptance, then subsequent retirement and GPR/SPR observations prove no killed effect. Six problem-state attempts cover reads and writes of all three selectors, with Program Priv SRR0/SRR1 and no seed mutation. The disabled profile checks all six selector forms retain the diagnostic illegal halt.

The focused build log is `/tmp/ppc-tlb-seed-tests.log` (exit 0, 2026-09-23). The canonical lint target is `make -C sim lint-tlb-seed`; its standalone core profile passed before the concurrent Round 3 router interface widened. The wrapper lint profile subsequently passed against the completed round-3 core/router wiring in `/tmp/ppc-tlb-load-focused.log`.
