# Depth references for the hdl-coding-guidelines skill

Source bundle: `references/source/` (next to `SKILL.md`) (23 files, bundle version 2026-05-19,
target Cyclone V SoC `5CSEBA6U23I7` / Quartus Standard/Lite 18.1). Do not modify the source files.
Every topic doc (10–41) uses the same section layout: §1 purpose, §2 contract (labelled [C]/[V]/[O]/[I]),
§3 constructs and verbatim templates, §4 sequencing/timing diagrams, §5 minimal working pattern,
§6 variations across implementations, §7 anti-patterns (Symptom→Cause→Fix→Citation), §8 verification,
§9 provenance. Jump to §2 for rules, §5 for copy-pasteable code, §7 for a bug you are chasing.

| File | What it gives you |
|---|---|
| `00-INDEX.md` | Bundle scope (what is and is not covered — MiSTer framework wrapper is out), claim-label semantics [C]/[V]/[O]/[I], reading order, one-line topic index with status mix per doc. |
| `01-glossary.md` | 165 terms: Cyclone V hardware (ALM, LAB, MLAB, M10K, DSP modes, GCLK/RCLK/PCLK, IOE, constant-zero power-up), SV constructs, CDC/reset vocabulary (MCP vs multicycle path split), handshake/FIFO terms, SDC commands, Quartus report row names, era-faithful terms, bundle conventions. |
| `02-source-map.md` | Citation hub: which upstream source (Intel docs, Cummings papers, FPGACPU, lowRISC, ZipCPU, verilog-axis, wb2axip, FPGADesignElements) backs which doc, with revisions and which are live-URL-only. Use to judge how authoritative a rule is. |
| `10-hardware-mindset-and-microarchitecture.md` | Circuits-not-algorithms mindset; concurrency of `always`/`assign`; for-loops elaborate to parallel hardware; the mandatory pre-RTL plan field table (§3.1) and cycle-level schedule; Core/Instance/Adapter/Shim hierarchy; anti-patterns #8, #40–#42. |
| `11-clocking-resets-and-cyclone-v-clock-networks.md` | Never derive clocks in fabric; clock-enable replaces gating; async-assert/sync-release reset with PLL-lock gating (§5 full pattern); polarity consistency; GCLK/RCLK/PCLK tiers and counts; reset-fanout analysis; pre-commit grep list (§8); anti-patterns #9–#12, #43. |
| `12-synthesizable-sv-subset.md` | Table A allowed constructs / Table B forbidden constructs with reasons; width and signedness rules W-1..W-5 with lowRISC examples; `always_comb` sensitivity gotcha; counter-with-clear minimal pattern; anti-patterns #5, #6, #44. |
| `13-registers-and-combinational-blocks.md` | Single driver, NBA-in-seq / blocking-in-comb, defaults-at-top latch avoidance, secondary control signal shapes that engage ALM CE/SCLR/ACLR/SLOAD, one-concern-per-block with a monolithic→split refactor example; Quartus warning strings to grep; anti-patterns #1–#4. |
| `14-finite-state-machines.md` | One/two/three-block FSM templates, `typedef enum logic` with explicit storage, default-state discipline, encoding table (binary/one-hot/Gray/Quartus-chosen) and how to pin encoding in QSF, microcoded ROM-driven FSM skeleton and its extra cycle of latency, transition-coverage and unenumerated-state tests; anti-patterns #7, #13, #14, #45. |
| `15-pipelines-and-latency-thinking.md` | Valid-follows-data contract, `(data, valid)` pair under one enable, backpressure freeze, cycle-level schedule tables, retiming limits, the cycle-accuracy boundary diagram for wrapped external interfaces, 3-stage minimal pipeline with schedule; anti-patterns #15, #16, #46. |
| `16-resource-and-state-economy.md` | The four-justifications rule for registers and bus bits, justification-comment convention, width-arithmetic table (`$clog2(N)`, `W + $clog2(K)`), DE10-Nano resource budget, `Width_Adjuster` narrowing primitive, Quartus report-reading table for Removed/Merged/Stuck-at; anti-patterns #17, #20, #27–#31. |
| `17-era-faithful-microarchitecture.md` | Mirror-the-chip contract, cycle-accuracy boundary (external locked / internal free), resource sharing as era default, one-hot bus mux replacing tri-state (the one [C]), shared-ALU + arbiter, microcoded vs hardwired controller skeletons, era-violating vs era-faithful ALU example, module-to-die-block map and cycle-trace gate; anti-patterns #32–#37 (+#25, #26 era angle). |
| `20-ready-valid-handshakes.md` | The three [C] rules (no valid-drop, payload-stable, transfer exactly on `valid && ready`), composability, direction-of-drive, `valid` must not depend on `ready`, four canonical waveforms plus the illegal one, ZipCPU producer/consumer templates, SVA pair, naming-style variants; anti-patterns #47–#50, #38 pointer. |
| `21-skid-buffers-and-register-slices.md` | When (and only when) to insert a skid buffer, exactly-two-entries rule, `o_ready = !r_valid` as the load-bearing line, `OPT_OUTREG`, stall waveform, verbatim wb2axip `skidbuffer.v`, chained slices vs FIFO, SRL variant not portable to Cyclone V; anti-patterns #18, #51, #52. |
| `22-fifos-synchronous-and-asynchronous.md` | Sync (binary pointer, extra MSB) vs async (Gray + 2FF + own-domain full/empty with the two-top-bits constant) FIFO cores from verilog-axis, backpressure via `tready = !full`, depth-from-burst×stall sizing, MLAB vs M10K sizing table, bug-symptom→root-cause cheat sheet; anti-patterns #38, #53, #54, #20/#21 pointers. |
| `23-cdc-single-bit.md` | 2FF chain rules: dedicated module, Quartus attributes (`PRESERVE`, `useioff=0`, `SYNCHRONIZER_IDENTIFICATION`), register-fed input, pulse→toggle→edge-detect synchronizer (2-phase/4-phase), SDC `set_clock_groups`/`set_false_path`, latency cases, Report Metastability/MTBF reading; anti-patterns #55–#59, #20. |
| `24-cdc-multi-bit.md` | Why bit-by-bit sync fails (phantom words), the three legal patterns (async FIFO / MCP word synchronizer / Gray counter) with a side-by-side selection table, Gray-before-crossing rule, payload-stable hold for MCP, quasi-static config exception, bus narrowing, clock-ratio stress test; anti-patterns #19, #21, #60–#62. |
| `30-memory-inference-cyclone-v.md` | Flops vs MLAB vs M10K decision procedure and template→resource table, verbatim Intel templates (new-data via blocking, old-data via nonblocking, case ROM, `$readmemb`), byte-enable shape, RDW timing diagrams, `ramstyle` attributes, flop register file for >2 read ports; anti-patterns #24, #25, #63–#65. |
| `31-dsp-inference-cyclone-v.md` | Registered-multiply template, multiply-add and MAC templates, signedness end-to-end, precision modes 27×27/18×19/9×9, pattern→DSP-mode table, when to instantiate IP, iterative shift-add era-faithful multiplier and exact-latency SVA, every-`*`-is-deliberate audit; anti-patterns #23, #26, #66–#68. |
| `32-arithmetic-patterns-and-operator-cost.md` | Per-operator cost tables (arithmetic; select/slice/mux/priority-encode), binary vs one-hot mux thresholds, saturation/wrap discipline, signed/unsigned trap, wide-comparator and priority-encoder pipelining patterns, saturating accumulator example; anti-patterns #22, #39, #69–#71. |
| `40-timing-closure-and-sdc.md` | SDC contract (`create_clock`, `derive_pll_clocks`, `derive_clock_uncertainty`, I/O delays with -max/-min, `check_timing`), false-path and multicycle discipline (setup N / hold N-1), `-from` vs `-to` reset false-path forms, IOE register QSF assignments, setup/hold/recovery/removal definitions, complete DE10-Nano SDC; anti-patterns #72–#75. |
| `41-quartus-reports-and-verification.md` | Post-compile report walk (Synthesis → Fitter → TimeQuest → sim log) with the warning strings and sections to open, three handshake SVA properties in concurrent form, bind-style `handshake_props` module, scoreboarded testbench skeleton, assert-vs-assume, invariant-not-recomputation rule, gates 6–8 evidence; anti-patterns #76–#80. |
| `90-anti-patterns.md` | All 80 anti-patterns (39 pre-committed + 41 addenda), each Symptom→Cause→Fix→Citation with primary-home doc; the single lookup table when you have a symptom and need the cause. |
| `91-core-bringup-checklist.md` | Ten sequential yes/no gates (plan, clock/reset, subset, FSM/pipeline, handshake/CDC, inference, timing, verification, era, economy) each item tied to a §2 rule and a named artifact to inspect. |
